import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.randomdogfacts.app',
  appName: 'random-dog-facts',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
